sidebar.sidebar_container
  .sidebar__top
    .brand_container
      = link_to root_path, class: 'brand' do
        = image_tag  "church_logo.gif", class: 'brand__logo'
        span.brand__name
          | Church of Pentecost
    .menu_items
      = menu_item(:pages, :home, 'Dashboard', nil,  {icon: 'meter'})
      = menu_item(:church_services, :index, 'Services', nil,  {icon: 'church'})
      = menu_item(:members, :index, 'Members', nil, {icon: 'users'})
      = menu_item(:events, :index, 'Events', events_path, {icon: 'clipboard'})
      = menu_item(:attendances, :index, 'Attendance', attendances_path, {icon: 'calendar'})
  .sidebar__bottom
    .menu_buttons
      = menu_item(:users, :show, 'My Profile', user_path(Current.user), {icon: 'user', class: 'profile'})
      = menu_item(:user_sessions, :logout, 'Log Out', nil, {icon: 'logout', class: 'logout', method: :delete})
